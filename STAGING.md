# Staging branch. Deploys to familyos-git-dev-guild-talent.vercel.app against the familyos-test database. Test login: demo@familyos.test / familyos123. Merge dev into main to ship to production. Google Calendar sync needs GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET in Vercel (Production + Preview).

Staging note: Preview carries the familyos-test service key (added Aug 25) so the AI connector works on staging.
